apiVersion: v1
kind: Service
metadata:
  name: starter-api
  namespace: replace-me
  labels:
    app: starter-api
spec:
  type: ClusterIP
  selector:
    app: starter-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
